[1mdiff --git a/src/service/user/auth/refreshUserService.ts b/src/service/user/auth/refreshUserService.ts[m
[1mindex aa66435..e75080c 100644[m
[1m--- a/src/service/user/auth/refreshUserService.ts[m
[1m+++ b/src/service/user/auth/refreshUserService.ts[m
[36m@@ -1,6 +1,5 @@[m
 import { signAccessToken, signRefreshToken, verifyRefreshToken } from "../../../lib/jwt.js";[m
 import { prisma } from "../../../lib/prisma.js";[m
[31m-import type { Prisma } from "../../../generated/prisma/client.js";[m
 import { hashToken } from "../../../lib/tokenHash.js";[m
 import { AuthServiceError } from "./authErrors.js";[m
 import type { AuthenticatedUser } from "./loginUserService.js";[m
[36m@@ -17,49 +16,43 @@[m [mclass RefreshUserService {[m
       const { userId } = await verifyRefreshToken(refreshToken);[m
       const incomingTokenHash = hashToken(refreshToken);[m
 [m
[31m-      const existingSession = await prisma.refreshSession.findUnique({[m
[31m-        where: { tokenHash: incomingTokenHash },[m
[31m-      });[m
[32m+[m[32m      const user = await prisma.user.findUnique({ where: { id: userId } });[m
 [m
[31m-      if (!existingSession) {[m
[31m-        await prisma.refreshSession.updateMany({[m
[31m-          where: { userId, revokedAt: null },[m
[31m-          data: { revokedAt: new Date() },[m
[31m-        });[m
[32m+[m[32m      if (!user) {[m
         throw new AuthServiceError("Unauthorized", 401);[m
       }[m
 [m
[31m-      if (existingSession.revokedAt || existingSession.expiresAt <= new Date()) {[m
[32m+[m[32m      const existingSession = await prisma.refreshSession.findUnique({[m
[32m+[m[32m        where: { tokenHash: incomingTokenHash, userId: userId },[m
[32m+[m[32m      });[m
[32m+[m
[32m+[m[32m      if (!existingSession) {[m
[32m+[m[32m        await prisma.refreshSession.deleteMany({ where: { userId } });[m
         throw new AuthServiceError("Unauthorized", 401);[m
       }[m
 [m
[31m-      const user = await prisma.user.findUnique({ where: { id: userId } });[m
[31m-[m
[31m-      if (!user) {[m
[32m+[m[32m      if (existingSession.expiresAt <= new Date()) {[m
[32m+[m[32m        await prisma.refreshSession.delete({ where: { id: existingSession.id } });[m
         throw new AuthServiceError("Unauthorized", 401);[m
       }[m
 [m
[32m+[m[41m      [m
       const newRefreshBundle = await signRefreshToken(user.id);[m
       const newTokenHash = hashToken(newRefreshBundle.token);[m
 [m
[31m-      await prisma.$transaction(async (tx: Prisma.TransactionClient) => {[m
[31m-        const createdSession = await tx.refreshSession.create({[m
[31m-          data: {[m
[31m-            userId: user.id,[m
[31m-            tokenHash: newTokenHash,[m
[31m-            expiresAt: newRefreshBundle.expiresAt,[m
[31m-          },[m
[31m-        });[m
[31m-[m
[31m-        await tx.refreshSession.update({[m
[31m-          where: { id: existingSession.id },[m
[31m-          data: {[m
[31m-            revokedAt: new Date(),[m
[31m-            replacedBySessionId: createdSession.id,[m
[31m-          },[m
[31m-        });[m
[32m+[m[32m      // Rotate the session row in place instead of inserting a new row per refresh,[m
[32m+[m[32m      // otherwise the table grows forever. Keying the update on the current tokenHash[m
[32m+[m[32m      // makes it atomic: only one concurrent request can match and win the rotation,[m
[32m+[m[32m      // the rest find 0 rows updated and fail with 401.[m
[32m+[m[32m      const rotated = await prisma.refreshSession.updateMany({[m
[32m+[m[32m        where: { id: existingSession.id, tokenHash: incomingTokenHash },[m
[32m+[m[32m        data: { tokenHash: newTokenHash, expiresAt: newRefreshBundle.expiresAt },[m
       });[m
 [m
[32m+[m[32m      if (rotated.count !== 1) {[m
[32m+[m[32m        throw new AuthServiceError("Unauthorized", 401);[m
[32m+[m[32m      }[m
[32m+[m
       const accessToken = await signAccessToken({[m
         sub: user.id,[m
         email: user.email,[m
